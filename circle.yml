machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t fulfiller .

test:
  override:
    - mkdir artefacts
    - docker run -v $CIRCLE_ARTIFACTS:/home/app/shippable fulfiller npm test
